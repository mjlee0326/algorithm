#include <string>
#include <vector>
#include <map>

using namespace std;

vector<int> solution(vector<string> genres, vector<int> plays) {
    vector<int> answer;

		map<int> m;


    return answer;
}

int main(){
	vector<int> answer;

	answer = solution({"classic", "pop", "classic", "classic", "pop"}, {500, 600, 150, 800, 255});
	cout<<answer<<endl;
}
